import { NextResponse } from "next/server";

/** Social receipt cards belonged to the retired click-campaign product. */
export async function GET() {
  return new NextResponse("Not found", { status: 404 });
}
